# Portal ACL — match portal configs auth.bootstrap_token (default: dev-bootstrap-token)
VITE_API_TOKEN=dev-bootstrap-token
# Optional org context header X-Org-Id (portal auth.bootstrap_org_id default: default)
# VITE_ORG_ID=default
