# Local / IDE
.idea/
.claude/
.cursor/
.learnings/
.superpowers/
*.local

# Build / OS
**/bin/
**/node_modules/
**/dist/
**/.DS_Store

__pycache__/
*.pyc

# Nested git metadata (should not exist after monorepo absorb; keep ignored if re-cloned)
framework/.git/
portal/.git/
web/.git/

# Local secrets / debug artifacts
tool.json
.playwright-cli/
**/.playwright-cli/
scripts/deploy_remote.py
# Real channel credentials live here (see gateway/configs/channels.example.yaml)
gateway/configs/channels.local.yaml

.worktrees/

# Large / local-only data dumps under portal
portal/internal/chat/data/
portal/internal/service/data/

# Scratch / debug artifacts (not part of deliverable)
_neo4j_q/
output/
**/*.exe
smoke.log*
*.log

# Eval run history (regenerable; baseline.json is the durable artifact)
evals/reports/live_results.jsonl
evals/reports/live_report.json
