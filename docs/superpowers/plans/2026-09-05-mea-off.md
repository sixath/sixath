# S27 MEA Off Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 删除无外部调用者的 `framework/mea` 包。

**Architecture:** 在 harness 用 `os.Stat("../mea")` 锁定目录不存在，然后 `git rm -r framework/mea`。

**Tech Stack:** Go（`framework/mea`、`framework/harness` 锁定测试）

**规格:** [`2026-09-05-mea-off-design.md`](../specs/2026-09-05-mea-off-design.md)

**分支:** 从 `feature/s26-hypertool-off` 切 `feature/s27-mea-off`。不要在 `main` 上改。PowerShell 无 HEREDOC。不要 `--no-verify`。不要提交 `_neo4j_q/`。

---

## File map

| 动作 | 路径 |
|------|------|
| 测试 | `framework/harness/mea_off_test.go` |
| 删除 | `framework/mea/` |

禁止：删 growth/hub；合 assembler。

---

### Task 1: 失败测试

- [ ] `TestMEAPackageRemoved`：`../mea` 必须不存在
- [ ] 先跑应失败

---

### Task 2: 删目录

- [ ] `git rm -r framework/mea`
- [ ] `cd framework && go test ./harness ./tool ./templates -count=1`
- [ ] **Commit** `fix(framework): drop unused mea package after default path unwired`

---

### Task 3: 回归

- [ ] 不要 merge/push，除非用户明确要求。
